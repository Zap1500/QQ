using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class soapHandler : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    // like a step event????
    void Update()
    {
        //check for left click

        // if left click = held
            // soap bar is held in cursor

        // if held and left click (on animal only)
            //spwn soap bubbles

        // if held and right click = notHeld
            //drops soap

    }
}
